Работа с тестами происходит следующим образом:
  Входные данные необходимо считать из 2 файлов вида: textN.1.txt и textN.2.txt,
    здесь N - это номер теста, а 1 и 2 - слои (множества прямоугольников)
  Затем выбирается производимая над множествами операция, она производится,
  Результат этой операции (получаемое множество) записывается в новый файл.

Если для этого теста есть контрольное значение (его можно найти в Tests/Results),
  то выглядит оно вот так: 
  testNmul.txt
    здесь N - это номер теста, a mul-производимая операция

  Возможные операции:
    mul - пересечение множеств (1*2)
    sum - объединение множеств (1+2)
    sub1 - вычитание 2ого слоя из 1ого (1-2)
    sub2 - вычитание 1ого слоя из 2ого (2-1)
